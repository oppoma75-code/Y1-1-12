# وضع الشفرة في دالة التحقق من السيادة
def access_sovereign_system(code):
    if code == "1121":
        print("تم تفعيل النظام.. البدء في تحليل البيانات الاستراتيجية.")
        # هنا يبدأ تشغيل منطق المليون دولار
    else:
        print("وصول مرفوض. النظام في وضع السكون.")
# الملف: security_logic.py

def validate_sovereign_key(input_key):
    # القانون التأسيسي: 1+1=12
    # الشفرة القائدة: 1121
    master_key = "1121"
    
    if input_key == master_key:
        return True
    else:
        # نظام التدمير الذاتي للمنطق في حال الإدخال الخاطئ
        return False
